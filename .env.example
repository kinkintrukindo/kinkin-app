# Supabase connection values are configured locally in .env.local and on Vercel.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Set this only for the Vercel Preview environment scoped to the staging branch.
VITE_READ_ONLY=false
